version: '3.8'
services:
  # 데이터베이스 설정
  postgres:
    image: postgres:16-alpine
    container_name: tt_postgres
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_INITDB_ARGS: "-E UTF8 --locale=C"
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - common

  # 레디스 설정
  redis:
    image: redis:alpine
    container_name: tt_redis
    restart: always
    #추후 보안을 위해 비밀번호 설정 가능
    #command: redis-server --requirepass ${REDIS_PASSWORD}
    ports:
      - "${REDIS_PORT:-6379}:6379"
    networks:
      - common

  # 스프링 부트 애플리케이션 설정
  app:
    image: ghcr.io/chehyeon-kim23/tt_backend:latest
    container_name: tt_app
    restart: always
    depends_on:
      - postgres
      - redis
    ports:
      - "8080:8080"
    environment:
      - DOPPLER_TOKEN=${DOPPLER_TOKEN}
    command: >
      sh -c "doppler run -- java -Dspring.profiles.active=prod -jar app.jar"

    networks:
        - common
networks:
  common:
    driver: bridge

volumes:
  postgres_data: